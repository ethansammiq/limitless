# Weather Edge — Dependencies
# Install: pip install -r requirements.txt
# Python 3.11+ required (for zoneinfo, modern type hints)

# Async HTTP client for API calls (NWS, Kalshi, IEM, Discord)
aiohttp~=3.9

# Environment variable management (.env files)
python-dotenv~=1.0

# RSA-PSS signing for Kalshi API authentication
cryptography~=42.0

# Retry logic with exponential backoff (Kalshi API)
tenacity~=8.2

# Test suite (deploy.sh runs it remotely after every sync)
pytest~=9.1
